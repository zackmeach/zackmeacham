---
import { navigation, site } from "../data/site";

interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;

function isActive(href: string) {
  if (href.startsWith("/#")) return false;
  if (href === "/") return currentPath === "/";
  return currentPath === href || currentPath.startsWith(href + "/");
}
---

<header class="shell pt-8 pb-2 sm:pt-10">
  <div class="flex flex-col gap-3 sm:flex-row sm:items-baseline sm:justify-between sm:gap-6">
    <a href="/" class="text-[0.95rem] font-medium tracking-tight text-ink">
      {site.name}
      <span class="mono ml-2 hidden sm:inline">/ software engineer</span>
    </a>
    <nav aria-label="Primary" class="flex flex-wrap items-center gap-x-4 gap-y-2 text-[13px] text-graphite sm:gap-x-5 sm:text-sm">
      {navigation.map((item) => (
        <a
          href={item.href}
          class="nav-link hover:text-ink transition-colors"
          data-active={isActive(item.href) ? "true" : "false"}
        >
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>
